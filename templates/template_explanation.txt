Пояснение к коду (НОМЕР_РАБОТЫ, Задача N.M Фамилия)

Уравнение / постановка: <вставить условие>.

Задача: <одно предложение о цели>. Метод тот же, что разбирался на лекциях.

restart;
with(DifferentialGeometry):with(JetCalculus):with(Tools):with(PDETools):
- Подключаем стандартные пакеты.

Preferences("JetNotation", "JetNotation2");
- Включаем нотацию y[0], y[1], ... в джет-пространстве.

DGsetup(...);
- Задаем многообразие струй.

E:=...;
- Левая часть исходного уравнения.

phi:=...;
- Эволюционная часть u_t = phi(x, u, u_x).

ode:=diff(y(x),x)-f(x,y(x));
F:=convert(ode,DGjet);
- Кандидат на инвариантное ОДУ первого порядка.

omega:=evalDG(dy[0]-f(x, y[0])*dx);
- Контактная 1-форма.

S:=evalDG(phi*D_y[0]);
- Эволюционное векторное поле.

sub_y1:={y[1]=f(x, y[0])};
S1:=eval(S,sub_y1);
- Ограничение S на подмногообразие y[1] = f.

evalDG(LieDerivative(S1,omega) &w omega);
- Условие инвариантности (совместимости).

G:=...;
pdsolve(G);
- Раскрытие условия совместимости и решение полученного PDE на f.

sub_f:={f(x, y[0]) = <выбранная функция>};
- Выбор простейшей нетривиальной f.

F1:=eval(F,sub_f); dsolve(convert(F1,DGdiff));
- Решение x-овского ОДУ.

S2:=eval(-S1,sub_f);
Xi:=simplify(Flow(S2,t));
v:=[y[0]-<начальный профиль>];
solve(Pullback(Xi,v),y[0]);
- Получение t-зависимости через поток.

sub_u:={u(t,x)=<итоговое решение>};
simplify(eval(E,sub_u));
- Проверка: результат должен быть 0.

plot(...); plot3d(...);
- Визуализация при конкретных параметрах.

Вывод:
- Условие совместимости: <PDE на f>.
- Выбранная инвариантная динамика: u_x = <f>.
- Точное решение: u(t, x) = <формула>.
